 'use strict';
 
angular.module('Navigate')
 
.controller('navigateController', ['$scope','$rootScope', function ($scope,$rootScope) {
    	//console.log($rootScope.globals.currentUser.username);
    	/*if($rootScope.globals.currentUser.username)
    		$rootScope.sessionName = $rootScope.globals.currentUser.username;*/
    }]);

